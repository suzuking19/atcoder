愚直に処理を実装するとTLEで失敗する
こういった場合は周期性を利用して計算慮を減らす

Aが必要となる最低の個数をもとめる
(Ａが必要となる最低の個数)=X//(Ａの要素の総和)

(Aが必要となる最低限の個数)のk(Bのインデックス番号+1)は次のようにして求まる
(Aが必要となる最低限の個数)*(A一つあたりの要素数)

これらをもとめたら、あとはAを順番に足していき、(Bのインデックス番号+1)を求める
